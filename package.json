{
  "name": "coverage-improver",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.5.3"
  },
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=24.0.0"
  }
}
